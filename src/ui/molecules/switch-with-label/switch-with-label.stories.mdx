import { boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

import { SwitchWithLabel } from '@ui';

<Meta title="Design System/molecules/SwitchWithLabel" component={SwitchWithLabel} />

# SwitchWithLabel

SwitchWithLabel is used to display the label next to the switch.

<Preview withToolbar>
  <Story name="default">
    <SwitchWithLabel
      label="Test"
      onBlur={action('switch-blur')}
      onFocus={action('switch-focus')}
      onChange={action('switch-change')}
      checked={boolean('Checked', false)}
    />
  </Story>
</Preview>
