import { boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

import { Switch } from '@ui';

<Meta title="Design System/atoms/Switch" component={Switch} />

# Switch

Switch is used to allow users to switch between two states.

<Preview withToolbar>
  <Story name="default">
    <Switch
      onBlur={action('switch-blur')}
      onFocus={action('switch-focus')}
      onChange={action('switch-change')}
      checked={boolean('Checked', false)}
    />
  </Story>
</Preview>
